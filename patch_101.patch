--- a/src/app/components/BeforeAfterSlider.tsx
+++ b/src/app/components/BeforeAfterSlider.tsx
@@ -6,6 +6,7 @@
   caption?: string;
   beforeAlt?: string;
   afterAlt?: string;
+  isMobileLayout?: boolean;
 };
 
 /**
@@ -19,6 +19,7 @@
   caption,
   beforeAlt = "Before the redesign",
   afterAlt = "After the redesign",
+  isMobileLayout = false,
 }: Props) {
   const [pos, setPos] = useState(50);
 
@@ -25,6 +25,6 @@
     <figure className="w-full flex flex-col items-center">
       {/* 1. Header Labels (Outside/Above the images) */}
-      <div className="flex justify-between w-full max-w-[400px] mb-4 px-2">
+      <div className={`flex justify-between w-full ${isMobileLayout ? "max-w-[400px]" : ""} mb-4 px-2`}>
         <span className="px-3 py-1 rounded-full border border-black/10 bg-white/50 text-black/60 text-[10px] uppercase tracking-[0.3em] backdrop-blur-md">
           Before
         </span>
@@ -33,8 +33,8 @@
       </div>
 
       {/* 2. Scrollable Container for the actual slider */}
-      <div className="w-full max-w-[400px] h-[75vh] overflow-y-auto overscroll-contain rounded-xl border border-black/10 bg-[#fafaf7] shadow-lg">
-        <div className="relative inline-block w-full overflow-hidden select-none">
+      <div className={`w-full ${isMobileLayout ? "max-w-[400px] h-[75vh] overflow-y-auto overscroll-contain rounded-xl border border-black/10 shadow-lg" : ""} bg-[#fafaf7]`}>
+        <div className={`relative inline-block w-full ${!isMobileLayout ? "max-w-full overflow-hidden" : ""} select-none`}>
           {/* Base layer: AFTER. No max-h restriction so it grows fully to its natural height */}
           <img
             src={after}
@@ -76,7 +76,7 @@
       </div>
 
       {caption && (
-        <figcaption className="pt-4 italic opacity-60 text-[13px] text-center max-w-[400px]">
+        <figcaption className={`pt-4 italic opacity-60 text-[13px] text-center ${isMobileLayout ? "max-w-[400px]" : ""}`}>
           {caption}
         </figcaption>
       )}
