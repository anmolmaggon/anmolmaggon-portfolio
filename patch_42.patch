--- a/src/app/components/CaseStudyModal.tsx
+++ b/src/app/components/CaseStudyModal.tsx
@@ -355,11 +355,11 @@
             </div>
           )}
 
-          {/* What we're watching — success criteria with targets */}
+          {/* What we're watching — directional community-health metrics */}
           {study.watching && (
             <div className="mb-24">
               <p className="font-sans font-medium text-[13px] text-black/50 uppercase tracking-wider mb-8">What we're watching</p>
-              <div className="grid grid-cols-1 sm:grid-cols-3 gap-6 mb-5">
+              <div className="grid grid-cols-2 md:grid-cols-4 gap-6 mb-5">
                 {study.watching.metrics.map((m) => (
                   <div key={m.label} className="py-5 border-t border-black/15">
                     <p
