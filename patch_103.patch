--- a/../../../.gemini/antigravity-ide/brain/c492a267-f4ae-42f9-a60d-1212cc4ca6f6/task.md
+++ b/../../../.gemini/antigravity-ide/brain/c492a267-f4ae-42f9-a60d-1212cc4ca6f6/task.md
@@ -7,11 +7,11 @@
   - `[x]` Delete the 3 bottom screenshots from the `shots` array.
   - `[x]` Add the new "Surfacing the gender gap" Design Decision.
   - `[x]` Add media placeholders to the Design Decisions.
-- `[ ]` **2. Slider Redesign (`BeforeAfterSlider.tsx`)**
-  - `[ ]` Constrain width to a mobile size (e.g., `max-w-[400px]`) and center it.
-  - `[ ]` Wrap the component in an `overflow-y-auto h-[75vh]` container so the slider scrolls natively.
-  - `[ ]` Move the "Before" and "After" pill tags OUTSIDE and ABOVE the images.
-  - `[ ]` Revert `CaseStudyModal.tsx` to use the Slider instead of `BeforeAfterScroll`.
-- `[ ]` **3. Impact Heading (`CaseStudyModal.tsx`)**
-  - `[ ]` Add the "The impact" heading above the sleek metric cards.
+- `[x]` **2. Slider Redesign (`BeforeAfterSlider.tsx`)**
+  - `[x]` Constrain width to a mobile size (e.g., `max-w-[400px]`) and center it.
+  - `[x]` Wrap the component in an `overflow-y-auto h-[75vh]` container so the slider scrolls natively.
+  - `[x]` Move the "Before" and "After" pill tags OUTSIDE and ABOVE the images.
+  - `[x]` Revert `CaseStudyModal.tsx` to use the Slider instead of `BeforeAfterScroll`.
+- `[x]` **3. Impact Heading (`CaseStudyModal.tsx`)**
+  - `[x]` Add the "The impact" heading above the sleek metric cards.
