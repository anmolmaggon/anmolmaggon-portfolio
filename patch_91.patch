--- a/src/app/components/BeforeAfterSlider.tsx
+++ b/src/app/components/BeforeAfterSlider.tsx
@@ -23,6 +23,7 @@
 }: Props) {
   const [pos, setPos] = useState(50);
 
+  return (
     <figure className="w-full flex flex-col items-center">
       <div className="relative inline-block max-w-full overflow-hidden rounded-none bg-[#fafaf7] select-none">
         {/* Base layer: AFTER (fills, shown on the right of the divider). Relative to dictate container height. */}
